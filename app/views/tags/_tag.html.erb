<div id="<%= dom_id tag %>" class="tag bg-blue-400 rounded-xl py-1 px-4 drop-shadow-sm <%= 'bg-blue-100' if !tag.active %>">
  <p>
    <strong><%= tag.name %></strong>  
  </p>
  <%= show_active_state(tag) %>
</div>
