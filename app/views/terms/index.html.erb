<div class="container pt-10">
  <h1>Terms <b><%= @terms.count %></b></h1>
  <div id="terms" class="flex flex-wrap gap-2 py-2">
    <% @terms.each do |term| %>
          <%= render term %>
    <% end %>
  </div>
</div>